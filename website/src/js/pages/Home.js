import React from 'react';
import { Link } from "react-router-dom";

const Home = () => {
    return (
        <div>

            <Link to="/presentation/">Lets see presentation</Link>
        </div>
    );
};

export default Home;